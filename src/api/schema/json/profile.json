{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "API profile",
    "type": "object",
    "properties": {
        "$schema": {
            "enum": ["api:profile"],
            "type": "string"
        },
        "uuid": {
            "type": "string"
        }
    },
    "additionalProperties": false,
    "required": ["$schema", "uuid"]
}
