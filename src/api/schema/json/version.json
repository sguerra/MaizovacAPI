{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "API version",
    "type": "object",
    "properties": {
        "$schema": {
            "enum": ["api:version"],
            "type": "string"
        },
        "version": {
            "enum": ["v1"],
            "type": "string"
        }
    },
    "additionalProperties": false,
    "required": ["$schema", "version"]
}
