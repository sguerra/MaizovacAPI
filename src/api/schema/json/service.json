{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "API service",
    "type": "object",
    "properties": {
        "$schema": {
            "enum": ["api:service"],
            "type": "string"
        },
        "uuid": {
            "type": "string"
        }
    },
    "additionalProperties": false,
    "required": ["$schema", "uuid"]
}
