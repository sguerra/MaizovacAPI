{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "API record",
    "type": "object",
    "properties": {
        "$schema": {
            "enum": ["api:record"],
            "type": "string"
        },
        "uuid": {
            "type": "string"
        }
    },
    "additionalProperties": false,
    "required": ["$schema", "uuid"]
}
