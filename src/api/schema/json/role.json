{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "API role",
    "type": "object",
    "properties": {
        "$schema": {
            "enum": ["api:role"],
            "type": "string"
        },
        "uuid": {
            "type": "string"
        }
    },
    "additionalProperties": false,
    "required": ["$schema", "uuid"]
}
