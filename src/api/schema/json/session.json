{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "API session",
    "type": "object",
    "properties": {
        "$schema": {
            "enum": ["api:session"],
            "type": "string"
        },
        "uuid": {
            "type": "string"
        }
    },
    "additionalProperties": false,
    "required": ["$schema", "uuid"]
}
